# === Binance API ===
BINANCE_API_KEY=your_api_key_here
BINANCE_SECRET_KEY=your_secret_key_here
BINANCE_PAPER=true

# === Trading Control ===
# Set to true ONLY when ready for real money
LIVE_TRADING=false

# === Claude Agent ===
# Option 1 (recommended for server/cloud): API key (no expiry, no refresh needed)
# Get from: https://console.anthropic.com/settings/keys
# ANTHROPIC_API_KEY=sk-ant-api03-...
#
# Option 2 (local dev): OAuth auto-configured by claude_auth.py
# CLAUDE_CONFIG_DIR is set automatically if running under Claude Code
# CLAUDE_CONFIG_DIR=/Users/you/.claude

# === FRED Macro Regime (optional) ===
# Get free key: https://fred.stlouisfed.org/docs/api/api_key.html
# Without this, macro regime defaults to GOLDILOCKS
# FRED_API_KEY=your_fred_api_key

# === Logging ===
LOG_LEVEL=INFO
# Override log directory (default: binance/logs/)
# TRADING_LOGS_DIR=/path/to/logs
